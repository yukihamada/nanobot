{
  "name": "nanobot-edge",
  "version": "1.0.0",
  "description": "Cloudflare Workers edge proxy for nanobot (chatweb.ai / teai.io)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
